import 'package:flutter/material.dart';
import 'package:hr_tech/Pages/Login/side_bar_login.dart';

class Login extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: SideBarLayoutLogin(),
    );
  }
}
